import { FilterRouter } from '@server/components/filter'
import { Request, Response } from '@@types/http'

export class CustomerFilter implements FilterRouter {
    async perform(req: Request, res: Response) {

    }
}